<% provide(:title, '') %>
<div class="center hero-unit">
	<% if user_signed_in? %>
	  <% provide(:title, 'Welcome ' + current_user.name) %>
	  <h2>Welcome to </h2>
	  <%= image_tag("ten4ten_logo.png", alt: "ten4ten") %>
	<% else %>
  	  <h1>Ten4Ten</h1>
	  <h2>Your best, right now.</h2>
	  <%= link_to "Sign up", new_user_registration_path, class: "btn btn-large btn-primary" %>
	<% end %>
</div>
